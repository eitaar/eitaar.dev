---
import type { SvgComponent } from "astro/types";

interface Props {
	heading: string;
	text: string;
	Icon: SvgComponent;
	iconPosition?: "left" | "right";
	iconSize?: number;
}
const { heading, text, Icon, iconPosition, iconSize } = Astro.props;
const size = iconSize ?? 100;
const isLeft = iconPosition === "left" || !iconPosition;
---

<div
	class="flex flex-col items-center gap-8 px-6 text-left md:items-start md:gap-12 md:px-12"
	class:list={isLeft ? "md:flex-row" : "md:flex-row-reverse"}
>
	<div class="flex items-start justify-center md:justify-start">
		<Icon
			class="fill-current text-content"
			height={size}
			width={size}
			aria-hidden="true"
			focusable="false"
		/>
	</div>

	<div class="flex w-full flex-col items-center gap-4 md:items-start md:gap-6">
		<h4 class="headline text-center md:text-left">{heading}</h4>
		<p class="text text-center leading-relaxed whitespace-pre-line text-subContent md:text-left">
			{text}
		</p>
	</div>
</div>
